This folder consist of a dataset csv and model.py files.
To run the module enter python module.py in your terminal.
Before running do check if the dependecies and necessary libraries are installed or not.
If necessary you can also create a virtual environment and run the module as well.